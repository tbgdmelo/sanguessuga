<title>{{titulo}}</title>
<div class="container">
    <br />
    <h3 class="text-center">Centro: {{nome_centro}}</h3>
    <hr>

    <div class="inline">
        <div class="row">
            {{#each estoques}}

            <div class="estoque col-3 text-black">
                <div class="d-inline p-2">Tipo: {{getNomeSangue id_sangue @root.sanguineos}}</div>
                <div class="progress caixa-sangue">
                    <div class="progress-bar progress-bar-striped bg-danger progress-bar-animated" role="progressbar"
                        style="width: 10%" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>
                </div>
                <div class="d-inline p-2">Nível: {{quantidade}}</div>

            </div>


            {{/each}}
        </div>
    </div>
</div>